<Window x:Class="E33Randomizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Randomizer Mod Configuration" Height="600" Width="800"
        MinHeight="500" MinWidth="700">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Main Tab Control -->
        <TabControl Grid.Row="0" Margin="10">
            
            <!-- Enemy Randomizer Tab -->
            <TabItem Header="Enemy Randomizer">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                        <!-- Randomize Encounter Sizes -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" Name="RandomizeEncounterSizesCheckBox" 
                                      Content="Randomize Encounter Sizes" 
                                      IsChecked="False" 
                                      Margin="0,10,10,10"
                                      FontSize="14"/>
                            <CheckBox Grid.Column="1" Name="EncounterSize1CheckBox" 
                                      Tag="1"
                                      Content="1" 
                                      IsChecked="True" 
                                      Margin="0,10,0,10"
                                      FontSize="14"
                                      Visibility="{Binding IsChecked, ElementName=RandomizeEncounterSizesCheckBox, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <CheckBox Grid.Column="2" Name="EncounterSize2CheckBox" 
                                      Tag="2"
                                      Content="2" 
                                      IsChecked="True" 
                                      Margin="0,10,0,10"
                                      FontSize="14"
                                      Visibility="{Binding IsChecked, ElementName=RandomizeEncounterSizesCheckBox, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <CheckBox Grid.Column="3" Name="EncounterSize3CheckBox" 
                                      Tag="3"
                                      Content="3" 
                                      IsChecked="True" 
                                      Margin="0,10,0,10"
                                      FontSize="14"
                                      Visibility="{Binding IsChecked, ElementName=RandomizeEncounterSizesCheckBox, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </Grid>
                        
                        <!-- Randomize Merchant Fights -->
                        <CheckBox Name="RandomizeMerchantFightsCheckBox" 
                                  Grid.Row="1"
                                  Content="Randomize Merchant Fights" 
                                  IsChecked="True" 
                                  Margin="0,10,0,10"
                                  FontSize="14"/>
                        
                        <!-- Include Cut Content -->
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,10"
                                    Grid.Row="2">
                            <CheckBox Name="IncludeCutContentCheckBox" 
                                      Content="Include Cut Content" 
                                      IsChecked="True" 
                                      FontSize="14"/>
                            <TextBlock Text="⚠️ Warning: Do NOT parry Gestral Bullies or Gestral Dragoons, as it will soft-lock the game." 
                                       Foreground="Orange" 
                                       FontWeight="Bold" 
                                       Margin="10,0,0,0"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding IsChecked, ElementName=IncludeCutContentCheckBox, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </StackPanel>
                        
                        <!-- Phase 2 Simon Dropdown -->
                        <Label Content="Replace Phase 2 Simon with Phase 1 in mandatory fights until after:" FontWeight="Bold" Margin="0,10,0,5"
                               Grid.Row="3"/>
                        <ComboBox Name="Phase2SimonComboBox" 
                                  Width="200" 
                                  HorizontalAlignment="Left" 
                                  Margin="0,0,0,20"
                                  Grid.Row="4">
                            <ComboBoxItem Content="Lune Tutorial" IsSelected="True" Tag="SM_Eveque_ShieldTutorial*1"/>
                            <ComboBoxItem Content="Eveque" Tag="GO_Curator_JumpTutorial*1"/>
                            <ComboBoxItem Content="Goblu" Tag="AS_PotatoBag_Boss"/>
                            <ComboBoxItem Content="Ultimate Sakapatate" Tag="QUEST_MatthieuTheColossus*1"/>
                            <ComboBoxItem Content="Gestral Tournament" Tag="EN_Francois"/>
                            <ComboBoxItem Content="Never (I don't think it's possible but you do you)" Tag="SM_Lancelier*1"/>
                            
                        </ComboBox>
                        
                        <!-- Custom Enemy Placement Button -->
                        <Button Name="CustomEnemyPlacementButton" 
                                Content="Customize Enemy Placements" 
                                Width="500" 
                                Height="50" 
                                FontSize="20"
                                HorizontalAlignment="Center" 
                                Margin="0,10,0,20"
                                Background="Aqua"
                                Click="CustomEnemyPlacementButton_Click"
                                Grid.Row="5"/>
                        
                        <!-- Generate Button -->
                        <Button Name="GenerateFromFileButton" 
                                VerticalAlignment="Bottom"
                                Content="Generate from file" 
                                Width="150" 
                                Height="35" 
                                Click="GenerateButton_Click"
                                Background="Gray"
                                Foreground="White"
                                FontWeight="Bold"
                                Visibility="Hidden"
                                Grid.Row="6"/>
                    </Grid>
            </TabItem>
            
            <!-- Item Randomizer Tab -->
            <TabItem Header="Item Randomizer" Visibility="Hidden">
                <StackPanel Margin="20">
                    <TextBlock Text="Item randomizer settings coming soon..." 
                               FontSize="16" 
                               Foreground="Gray" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"/>
                </StackPanel>
            </TabItem>
            
            <!--~1~ Misc Tab @1@
            <TabItem Header="Misc">
                <StackPanel Margin="20">
                    <TextBlock Text="Miscellaneous settings coming soon..." 
                               FontSize="16" 
                               Foreground="Gray" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"/>
                </StackPanel>
            </TabItem>-->
            
        </TabControl>
        
        <!-- Bottom Section -->
        <Border Grid.Row="1" Background="#F0F0F0" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Seed Input -->
                <Label Grid.Column="0" Content="Seed:" VerticalAlignment="Center" FontWeight="Bold"/>
                <TextBox Name="SeedTextBox" 
                         Grid.Column="1" 
                         Margin="10,0,20,0" 
                         VerticalAlignment="Center" 
                         Height="25"
                         PreviewTextInput="SeedTextBox_PreviewTextInput"/>
                
                <!-- Preset Name Input -->
                <Label Grid.Column="2" Content="Preset Name:" VerticalAlignment="Center" FontWeight="Bold" Visibility="Hidden"/>
                <TextBox Name="PresetNameTextBox" 
                         Grid.Column="3" 
                         Margin="10,0,20,0" 
                         VerticalAlignment="Center" 
                         Height="25"
                         Visibility="Hidden"
                         PreviewTextInput="PresetNameTextBox_PreviewTextInput"/>
                
                <!-- Generate Button -->
                <Button Name="GenerateButton" 
                        Grid.Column="4" 
                        Content="Generate" 
                        Width="100" 
                        Height="35" 
                        Click="GenerateButton_Click"
                        Background="#4CAF50"
                        Foreground="White"
                        FontWeight="Bold"/>
                
            </Grid>
        </Border>
        
    </Grid>
</Window>